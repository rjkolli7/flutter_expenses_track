name: profile
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=2.19.0-374.0.dev <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_integration:
    path: ../../firebase_integration
  components:
    path: ../../core/components
  get: ^4.6.5

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
